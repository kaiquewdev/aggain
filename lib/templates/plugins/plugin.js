// file: plugins/{{name}}.js - created at {{timestamp}}
function {{name}}Handler(schema, options) {
  options = options || {};

  // start with plugin here
}
module.exports = exports = {{name}}Handler;
